<div id="Edicao" class="form-container">
    <h1 id="titulo">Editar Produto: {{produto.nome}}</h1>
    
    <form action="{{action}}" method="POST"> 
        
        <input type="hidden" name="id" value="{{produto.id}}">

        <div>
            <label for="nome">Nome do Produto:</label>
            <input type="text" id="nome" name="nome" value="{{produto.nome}}" required>
        </div>
        
        <div>
            <label for="preco">Preço (R$):</label>
            <input type="number" id="preco" name="preco" value="{{produto.preco}}" step="0.01" min="0.01" required>
        </div>
        
        <button type="submit">Salvar Alterações</button>
        
    </form>
    
    <p><a href="/cardapio">↩️ Cancelar e Voltar</a></p>
</div>